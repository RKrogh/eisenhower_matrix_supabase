// Copy this file to config.js and fill in your values, or set the
// SUPABASE_URL / SUPABASE_ANON_KEY env vars and run: node generate-config.mjs
// Values are in Supabase Dashboard > Settings > API.
//
// The anon (publishable) key is meant to ship to the browser; row-level
// security on the tasks table is the real access boundary, not this key.
export const SUPABASE_URL = 'https://your-project-ref.supabase.co';
export const SUPABASE_ANON_KEY = 'your-anon-key-here';
